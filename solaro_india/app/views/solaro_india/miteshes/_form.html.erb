<%= form_for(@mitesh) do |f| %>
  <% if @mitesh.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@mitesh.errors.count, "error") %> prohibited this mitesh from being saved:</h2>

      <ul>
      <% @mitesh.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
